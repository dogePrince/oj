#include<stdio.h>

int toValid()

int main(int argc, char const *argv[]) {
    int x, y, n, m, L;
    scanf("%d%d%d%d%d", &x, &y, &n, &m, &L);

    int delta = x - y;
    int step_delta = n - m;

    return 0;
}
